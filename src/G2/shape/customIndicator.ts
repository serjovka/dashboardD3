export const CustomIndicator = (style, context) => {
    const { document } = context;
    return (P, value, defaults) => {
        return document.createElement('rect', {
            
        });
    };
  }